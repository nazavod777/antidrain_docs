{
  "private": true,
  "scripts": {
    "build": "vitepress build .",
    "check": "npm run build && npm run check:build-output && npm run check:browser",
    "check:browser": "node ./scripts/browser-smoke.mjs",
    "check:build-output": "node ./scripts/check-build-output.mjs",
    "dev": "vitepress dev .",
    "preview": "node ./scripts/preview.mjs --host 127.0.0.1 --port 4175"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
